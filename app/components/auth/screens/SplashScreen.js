import React from 'react';
import { View, Text } from 'react-native';

//Pantalla que se muestra al cargar la app
export const SplashScreen = () => (
	<View>
		<Text> Inicio cutre! </Text>
	</View>
);
